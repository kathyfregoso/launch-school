let foo = ['a', 'b', 'c'];
foo.length;  // returns 3
foo[3];      // will this result in an error?

// No, it will return undefined (not an error) because there is no value assigned to the variable foo.