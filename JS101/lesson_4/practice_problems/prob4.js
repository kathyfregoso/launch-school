["ant", "bear", "caterpillar"].pop().length;
// returns 11

// the pop method destructively removes the last element of the array and returns that element
// the length property returns length of the return value of pop, which is "caterpillar"
