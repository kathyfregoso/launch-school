[1, 2, 3].map((num) => num * num);
// this returns [1, 4, 9]

// arrow functions with one expression allow for implicit returns
// the return value of the callback is a product (computed valued) of the transformation criteria (num * num)
// this would not work if curly braces surrounded the body of the arrow function
