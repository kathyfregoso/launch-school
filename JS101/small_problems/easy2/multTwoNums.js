// INPUTS:
// 2 numbers

// OUTPUTS:
// product of 2 numbers

// EXPLICIT REQUIREMENTS:
// Create a function that takes two arguments, multiplies them together, and returns the result.

// IMPLICIT REQUIREMENTS:
// the two arguments are numbers, the output is a number

// EXAMPLES / TEST CASE:
// console.log(multiply(5, 3) === 15); // logs true

// DATA STRUCTURE:
// numbers

// ALGORITHM:
// multiply the two input numbers together with the * operator, return the result

function multiply(num1, num2) {
  return num1 * num2;
}

console.log(multiply(5, 3) === 15); // logs true
